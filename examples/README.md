# Examples

Example POIDs demonstrating the format.

| Example | Shows | Status |
|---|---|---|
| [`kanban/`](kanban/) | The Data Engine: `poid.db.docs`, storage-mode independence, updates | ✅ runnable (M10) |
| notepad | `poid.db.kv`, embedded storage | planned |
| survey | `type: data` collection workflow (SPEC §11) | planned |
| python-chart | `web+python` profile, Pyodide | planned |

Each example is a project directory you can pack with `poid pack <dir>`.
Examples land together with the milestones that make them runnable.
