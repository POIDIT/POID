# Examples

Example POIDs demonstrating the format: notepad, kanban, survey, python-chart, cad.

Status: empty (M00). Examples land together with the milestones that make them
runnable (container packing, Reader, engines).
