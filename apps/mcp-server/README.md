# @poid/mcp-server

MCP server exposing poid_create / poid_validate / poid_inspect, so AI agents can emit .poid files.

```
pnpm --filter @poid/mcp-server build
pnpm --filter @poid/mcp-server test
```

Status: stub (M00). Tool implementations land in a later milestone.
