/**
 * @poid/mcp-server — MCP server exposing poid_create / poid_validate / poid_inspect, so AI agents can emit .poid files.
 */

/** Workspace package identifier. */
export const PACKAGE_NAME = "@poid/mcp-server";
