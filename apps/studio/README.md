# @poid/studio

POID Studio - the one executable (Tauri v2): Reader windows and the Studio hub.

```
pnpm --filter @poid/studio build
pnpm --filter @poid/studio test
```

Status: stub (M00). This is the frontend stub; the Tauri v2 shell (Rust side, windows) lands in the milestone that opens the first window.
