/**
 * @poid/cloud — POID Cloud - Cloudflare Workers backend services (Sync, Publish, Collect, DB, Files, Auth). The server never executes user code.
 */

/** Workspace package identifier. */
export const PACKAGE_NAME = "@poid/cloud";
