# @poid/cloud

POID Cloud - Cloudflare Workers backend services (Sync, Publish, Collect, DB, Files, Auth). The server never executes user code.

```
pnpm --filter @poid/cloud build
pnpm --filter @poid/cloud test
```

Status: stub (M00). Workers scaffolding lands in a later milestone.
