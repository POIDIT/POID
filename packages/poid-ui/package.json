{
  "name": "@poid/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Shared UI components: Reader chrome, consent dialog, progress bars.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "happy-dom": "^15.0.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
