# @poid/ui

Shared UI components: Reader chrome, consent dialog, progress bars.

```
pnpm --filter @poid/ui build
pnpm --filter @poid/ui test
```

Status: stub (M00). Components land in a later milestone.
