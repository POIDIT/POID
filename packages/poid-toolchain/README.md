# @poid/toolchain

In-app build pipeline: esbuild-wasm, the Standard Library and the dependency Resolver. No Node.js anywhere in the product.

```
pnpm --filter @poid/toolchain build
pnpm --filter @poid/toolchain test
```

Status: stub (M00). esbuild-wasm integration lands in a later milestone.
