# @poid/web

The Web Reader - a static, 100% client-side SPA for recipients who have not installed Studio.

```
pnpm --filter @poid/web build
pnpm --filter @poid/web test
```

Status: stub (M00). The SPA shell lands in a later milestone.
