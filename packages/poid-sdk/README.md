# @poid/sdk

The API injected into the sandboxed context as window.poid. Zero runtime dependencies; must stay under 10 KB minified.

```
pnpm --filter @poid/sdk build
pnpm --filter @poid/sdk test
```

Status: stub (M00). The real SDK surface (RUNTIME-API.md) lands in a later milestone.
