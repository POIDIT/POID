/**
 * @poid/sdk — The API injected into the sandboxed context as window.poid. Zero runtime dependencies; must stay under 10 KB minified.
 */

/** Workspace package identifier. */
export const PACKAGE_NAME = "@poid/sdk";
