# @poid/host

Host side of the postMessage bridge between the sandboxed iframe and the Reader window.

```
pnpm --filter @poid/host build
pnpm --filter @poid/host test
```

Status: stub (M00). The bridge protocol lands in a later milestone.
