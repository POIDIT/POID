# poid-cli

`poid` — the command-line tool: pack, unpack, validate and inspect `.poid` files.
Uses the native `esbuild` binary as a sidecar for builds (never Node.js).

```
cargo run -p poid-cli
```

Status: stub (M00). Prints its version. Subcommands land in a later milestone.
