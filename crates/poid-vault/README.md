# poid-vault

The vault: Automerge (CRDT) storage for POID application state, slots (multi-memory)
and instance identity / copy detection (SPEC §6).

```
cargo test -p poid-vault
```

Status: stub (M00). The Automerge engine lands in a later milestone.
